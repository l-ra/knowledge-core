apiVersion: v1
kind: Service
metadata:
  name: {{ include "knowledge-core.fullname" . }}
  labels:
    {{- include "knowledge-core.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "knowledge-core.selectorLabels" . | nindent 4 }}
